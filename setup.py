"""Setup configuration for linkedin-spider package."""
from setuptools import setup

# This setup.py is for backward compatibility.
# The actual configuration is in pyproject.toml using Poetry.
# This allows the package to be installed via:
# - pip install .
# - pip install git+https://github.com/alexcolls/linkedin-spider.git
# - python setup.py install (legacy)

setup()
